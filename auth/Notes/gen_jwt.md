# 🔐 Генерация ключей для JWT с помощью OpenSSL

Когда тебе нужно создавать и подписывать JWT-токены вручную (например, для собственного backend-сервиса), тебе нужна пара ключей:

Приватный (private key) — используется для подписи токена.

Публичный (public key) — используется для проверки подписи (верификации) токена.

# 🧩 Команда 1
openssl genrsa -out jwt-private.pem 2048

📘 Разбор:

openssl — утилита для работы с криптографией и сертификатами.

genrsa — команда для генерации RSA-приватного ключа.

-out jwt-private.pem — указывает, куда сохранить ключ (файл jwt-private.pem).

2048 — длина ключа в битах.

Это длина RSA-ключа, чем она больше — тем выше безопасность.

2048 бит — стандартный безопасный размер.

# 🧾 Результат:

Файл jwt-private.pem, содержащий закрытый RSA-ключ в PEM-формате (base64 + метки -----BEGIN PRIVATE KEY-----).

# 🧩 Команда 2
openssl rsa -in jwt-private.pem -outform PEM -pubout -out jwt-public.pem

📘 Разбор:

openssl rsa — команда для обработки RSA-ключей (чтение, конвертация, извлечение).

-in jwt-private.pem — указывает, из какого приватного ключа извлекать информацию.

-outform PEM — задаёт формат вывода: PEM (человеко-читаемый base64).

-pubout — говорит OpenSSL:
# 👉 «Извлеки публичный ключ из приватного и выведи его».

-out jwt-public.pem — сохраняет публичный ключ в файл jwt-public.pem.

🧾 Результат:

Файл jwt-public.pem, содержащий открытый RSA-ключ в PEM-формате (base64 + метки -----BEGIN PUBLIC KEY-----).

⚙️ В итоге у тебя есть два файла:
Файл	Назначение	Используется для
jwt-private.pem	Приватный ключ	Подпись JWT (на сервере)
jwt-public.pem	Публичный ключ	Проверка подписи JWT (на клиенте или другом сервисе)

# 🔐 Как это работает в контексте JWT

Сервер подписывает токен приватным ключом:

jwt.encode(payload, private_key, algorithm="RS256")


Клиент или другой сервис проверяет подпись публичным ключом:

jwt.decode(token, public_key, algorithms=["RS256"])


Без приватного ключа подделать подпись невозможно — безопасность обеспечена.

# 🧠 Кратко: что происходит по шагам

Создаётся RSA-приватный ключ (2048 бит).

Из приватного извлекается публичный ключ.

Оба сохраняются в PEM-файлы.

Приватный используется для подписи, публичный — для проверки.


🔐 Пример: создание и проверка JWT с RSA-ключами
📂 Предварительно

У тебя уже есть два файла, созданные ранее:

jwt-private.pem — приватный ключ

jwt-public.pem — публичный ключ

🧩 Шаг 1. Установка библиотеки
pip install pyjwt

🧩 Шаг 2. Код примера
import jwt
from datetime import datetime, timedelta

# Загружаем ключи
with open("jwt-private.pem", "r") as f:
    private_key = f.read()

with open("jwt-public.pem", "r") as f:
    public_key = f.read()

# Пример полезной нагрузки токена (payload)
payload = {
    "user_id": 123,
    "username": "alice",
    "role": "admin",
    "exp": datetime.utcnow() + timedelta(minutes=10)  # срок действия токена 10 минут
}

# 1️⃣ Создаём токен — подписываем приватным ключом
token = jwt.encode(payload, private_key, algorithm="RS256")

print("🔸 JWT токен:")
print(token)
print()

# 2️⃣ Проверяем токен — верифицируем публичным ключом
try:
    decoded = jwt.decode(token, public_key, algorithms=["RS256"])
    print("✅ Токен действителен. Расшифрованное содержимое:")
    print(decoded)
except jwt.ExpiredSignatureError:
    print("❌ Срок действия токена истёк.")
except jwt.InvalidTokenError:
    print("❌ Недействительный токен.")

🧠 Что происходит в коде:

Чтение ключей из PEM-файлов.

Создание JWT-токена с помощью приватного ключа и алгоритма RS256 (RSA + SHA-256).

Верификация токена — библиотека использует публичный ключ для проверки подписи.

При успешной проверке ты получаешь декодированный payload.

При истёкшем или поддельном токене — ошибка.

⚙️ В реальной практике:

Приватный ключ (jwt-private.pem) хранится только на сервере — никогда не публикуется.

Публичный ключ (jwt-public.pem) можно распространять (например, в другом микросервисе, который должен проверять токены).