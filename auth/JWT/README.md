```shell
# Генерирует приватный ключ RSA размером 2048 и дает ему название jwt-private.pem (название может быть обсолютно любым)

openssl genrsa -out jwt-private.pem 2048

# genrsa - говорит о том что мы генерируем RSA

# -out - говорит, что результат мы записываем в файл с названием jwt-private.pem, если файла нет, то он создается.

# jwt-private.pem - названиве файла с расширением pem

# 2048 - размер

```shell
# На основе приватного ключа с таким названием jwt-private.pem делаем публичный ключ с названием jwt-public.pem.

openssl rsa -in jwt-private.pem -outform PEM -pubout -out jwt-public.pem

# openssl rsa — команда для обработки RSA-ключей (чтение, конвертация, извлечение).

# -in jwt-private.pem — указывает, из какого приватного ключа извлекать информацию.

# -outform PEM — задаёт формат вывода: PEM (человеко-читаемый base64).

# -pubout — говорит OpenSSL:
# 👉 «Извлеки публичный ключ из приватного и выведи его».

# -out jwt-public.pem — сохраняет публичный ключ в файл jwt-public.pem.

# 🧾 Результат:

#Файл jwt-public.pem, содержащий открытый RSA-ключ в PEM-формате (base64 + метки -----BEGIN PUBLIC KEY-----).


# Клиент при помощи поубличного ключа проверяет данные, отправленные ему при помощи приватного ключа и проверяет тем ли именно приватным ключем это сделано. Тоесть ключи всегда выпускаются парами - приватный и публичный